flask==3.0.0
flask-cors==4.0.0
flask-pymongo==2.3.0
pymongo==4.6.1
torch==2.1.0
torchvision==0.16.0
ultralytics==8.0.200
opencv-python==4.8.1.78
easyocr==1.7.0
Pillow==10.1.0
numpy==1.24.3
werkzeug==3.0.1
python-dotenv==1.0.0
```

4. Press `Ctrl + S` to save

---

### **Step 3: Create Folders**

1. In VS Code left sidebar, click the "New Folder" icon
2. Create these folders one by one:
   - `templates`
   - `models`
   - `uploads`
   - `results`

Your folder structure should now look like:
```
anpr_project/
├── app.py
├── requirements.txt
├── templates/
├── models/
├── uploads/
└── results/